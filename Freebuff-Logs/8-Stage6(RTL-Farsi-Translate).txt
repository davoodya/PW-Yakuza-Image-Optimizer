################################
########### Question ###########
################################
Translate the plugin's admin UI into Farsi with full RTL support (the plugin author is Persian), using the settings tabs' existing text and proper text domain loading.



################################
############ Answer ############
################################
Translating the admin UI into Farsi — this is an i18n project: wrap every UI string in translation functions, load the text domain, ship a proper `fa_IR` .po/.mo, and add RTL-aware styles for the custom progress/log UI. Planning:

Starting with the main file — text domain loading and translated requirement notices:




Main file done. Now rewriting the admin UI files with translation wrappers — starting with the page shell and General tab:




Continuing — watermark and SEO tabs:




Now the two feature tabs — Backup and Bulk — with translations, RTL-aware CSS, and localized JS strings:




Resuming the Farsi translation stage — the admin-page, general, watermark, seo, and backup tabs are done. Now the Bulk tab (translations + RTL CSS + localized JS), then the AJAX message strings in the engines, then the .po/.mo files.

Let me first read the current state of the files I still need to touch:

The backup tab already has the i18n pattern I'll mirror. Now let me read the other translated admin files to build an exact string catalog for the .po file, and check what's still in English:

The admin files are all translated except the Bulk tab. Now let me check `settings.php` for any user-visible strings I need to catalog, then write the translated Bulk tab:

The Bulk tab is the last untranslated admin file. I'll now write it (mirroring the backup tab's i18n pattern with RTL CSS), localize the AJAX log strings in both engines, and rewire the menu registration to `admin_menu` (the current `plugins_loaded` registration evaluates `__()` before the text domain loads on `init` — a real i18n timing bug).




Bulk tab is now translated. Next: localize the AJAX log messages in both engines so the run logs appear in Farsi too:




Now localizing the restore engine's AJAX messages:




Both engines' user-visible strings are now localized. Updating the plan and fixing one i18n timing bug I spotted: the menu is registered on `plugins_loaded` but the text domain loads on `init` — so menu labels would evaluate before translations load. Fixing that now, and extracting the complete string catalog for the .po file:

Now fixing the text-domain timing bug in the main file — the menu is registered on `plugins_loaded`, but the text domain only loads on `init`, so menu labels would evaluate before translations exist. Moving menu registration to the proper `admin_menu` hook:




Now let me extract the complete string catalog to ensure the .po covers every translatable string:

The first pattern errored (unclosed regex group) — let me capture the plain `__()` calls too:

All strings cataloged. Now creating the Farsi translation file with the complete catalog (headers, all admin UI strings, JS strings, and AJAX messages):




The .po catalog is complete. Now I need to compile it to `.mo` — and since the authoritative reader here is WordPress's POMO class, let me verify its exact expectations before writing the compiler:

The POMO reader is now fully understood: header is 7 × 32-bit little-endian ints, `total = entries + 1` (entry 0 is the headers block), length excludes the NUL, and `hash_length = 0` is supported. I'll write a compiler + verifier that mirrors POMO's exact `import_from_reader` logic, and cross-check every `__()` string in the source against the catalog:




The .mo compiled and round-tripped perfectly — all 145 entries identical, and every source string is covered. Now validating everything: PHP lint across all files, JS syntax check on both tabs' inline scripts, then cleanup:

Lint is clean (no failures). The JS check tripped on PHP tags inside the extracted script — I need to strip `<?php ... ?>` segments before syntax-checking. Fixing the extraction:

JS check passed, but let me verify both files individually with explicit output to be certain:

Both JS blocks are valid. Now cleaning up the compiler script and running the final code review:

All my changes are in place. The `Freebuff-Logs/` files are the user's own working notes — I'll leave those untouched. Final step: code review of the i18n work:

Review is in — no functional bugs, one concrete cleanup worth applying: the unused `i18n.ready` key in both tabs. Removing it:




Both dead keys removed (the `Ready.` HTML string stays, so its .po entry remains valid). Re-validating the two modified tabs:

All checks pass. Marking the stage complete:

## Farsi Translation + RTL Support Complete ✅

The plugin's admin UI is now fully translated into Persian (`fa_IR`) with complete RTL support.

**Text domain loading (main file):**
- `load_plugin_textdomain()` wired on `init` with the `languages/` path — matching the plugin header's `Text Domain` / `Domain Path`.
- **Fixed a real i18n timing bug**: the admin menu was registered on `plugins_loaded`, which fires *before* `init` — so menu labels would have been evaluated before any translations loaded. The menu now registers on the proper `admin_menu` hook, guaranteeing translated labels.

**All UI strings wrapped (`__()` / `esc_html__()` / `esc_attr__()`):**
- Admin page shell, tab nav, and all five settings tabs: General, Watermark, SEO, Backup, and Bulk.
- Both run dashboards (Bulk + Restore) now pass their JS strings through a localized `$yio_i18n` object via `wp_json_encode()` — so counters, buttons, and live-log prefixes all render in Farsi.
- AJAX responses in both engines (`Insufficient permissions.`, `No images found to optimize.`, per-image log lines) are now localized with proper `%1$d`/`%2$s` sprintf placeholders, so the run logs read in Farsi too.

**RTL-aware styling:** scoped `[dir="rtl"]` rules for the custom progress bar and log panels (WordPress core already handles the form tables and tabs).

**The catalog (`languages/yakuza-image-optimizer-fa_IR.po` + compiled `.mo`):**
- **145 entries**, all translated into Persian with a correct `nplurals=1; plural=0` header and UTF-8 encoding.
- I wrote a custom `.mo` compiler that mirrors WordPress POMO's `export_to_file_handle()` byte-for-byte, then verified it with a faithful port of POMO's `import_from_reader()`: **perfect round-trip, all 145 entries identical**. I also cross-checked every `__()` call in the PHP sources against the catalog — **100% coverage, zero missing strings**.

**Validation:** `php -l` clean on all files, `node --check` passes on both tabs' inline JS, and code review confirmed the timing fix, escaping, and placeholder fidelity (its one concrete finding — an unused `i18n.ready` key — was applied).

One note: text *watermark* rendering still depends on server fonts for Persian glyphs (as flagged in Stage 2) — unrelated to the UI, which is fully RTL. The next natural milestone would be making that Persian text-watermark path robust: